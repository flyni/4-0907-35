#include <stdio.h>

int main() {
  int blue_balls[5] = {11, 12, 13, 14, 15}; // Предварительное заполнение
  int size = sizeof(blue_balls) / sizeof(blue_balls[0]);
  printf("В коробке blue_balls помещается %d шариков.\n", size);
  return 0;
}
